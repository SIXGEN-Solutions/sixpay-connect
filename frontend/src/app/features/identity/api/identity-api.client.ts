/**
 * TO_DEFINE.
 *
 * No authoritative Identity administration API contract was identified on the Phase 7
 * branch. Authentication remains OIDC/standalone through core/auth. This feature must
 * not invent IAM mutation endpoints or duplicate the identity provider.
 */
export {};
