spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sixpay
    username: sixpay
    password: sixpay
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

logging:
  level:
    org.hibernate.SQL: WARN
