/**
 * Inbound HTTP boundary of the Payment module.
 *
 * <p>This package will contain contract-derived controllers, request and
 * response models, HTTP security policies, validation and API error mapping.
 * It must delegate business execution to application input ports and must not
 * access persistence or external adapters directly.</p>
 */
package com.sixpay.payment.api;
