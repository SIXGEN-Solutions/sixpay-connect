/**
 * JPA persistence implementation for complete Payment aggregate state.
 *
 * <p>The package owns relational entities, Spring Data access, JSONB state
 * mapping and the adapter implementing the framework-free domain repository.
 * It must not contain application workflow orchestration.</p>
 */
package com.sixpay.payment.infrastructure.persistence;
