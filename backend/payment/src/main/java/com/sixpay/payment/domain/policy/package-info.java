/**
 * Pure deterministic Payment policies, approved immutable policy profiles and
 * typed decision records.
 *
 * <p>No class input this package performs I/O, reads the system clock, mutates the
 * Payment Aggregate Root or registers domain events.</p>
 */
package com.sixpay.payment.domain.policy;
