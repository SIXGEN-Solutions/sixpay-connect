/**
 * Pure Payment domain model.
 *
 * <p>This package owns the Payment Aggregate Root, immutable identifiers and
 * Value Objects, bounded evidence, policies, Domain Services, domain
 * exceptions and domain events. It remains independent from Spring,
 * persistence, transport and every other business module.</p>
 */
package com.sixpay.payment.domain;
