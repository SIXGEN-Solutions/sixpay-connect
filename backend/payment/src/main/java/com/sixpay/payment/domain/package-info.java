/**
 * Pure Payment domain model.
 *
 * <p>This package owns immutable identifiers and Value Objects, minimized
 * evidence snapshots, the future Payment Aggregate Root, policies, Domain
 * Services, domain exceptions and domain events. It remains independent from
 * Spring, persistence, transport and every other business module.</p>
 */
package com.sixpay.payment.domain;
