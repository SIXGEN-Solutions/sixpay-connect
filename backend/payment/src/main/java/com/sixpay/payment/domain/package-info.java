/**
 * Pure Payment domain model.
 *
 * <p>This package owns the Payment Aggregate Root, its Value Objects,
 * immutable evidence, policies, Domain Services, domain exceptions and
 * domain events. It must remain independent from Spring, persistence,
 * transport and every other business module.</p>
 */
package com.sixpay.payment.domain;
