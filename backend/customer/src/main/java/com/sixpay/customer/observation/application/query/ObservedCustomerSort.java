package com.sixpay.customer.observation.application.query;

/**
 * Stable sorts supported by the internal Observed Customer query contract.
 */
public enum ObservedCustomerSort {
    FIRST_OBSERVED_AT_ASC,
    FIRST_OBSERVED_AT_DESC,
    LAST_OBSERVED_AT_ASC,
    LAST_OBSERVED_AT_DESC
}
