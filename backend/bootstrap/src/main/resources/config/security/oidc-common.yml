spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${SIXPAY_IDP_ISSUER_URI}
          jwk-set-uri: ${SIXPAY_IDP_JWK_SET_URI}

sixpay:
  security:
    authentication:
      oidc:
        registration-id: sixpay
