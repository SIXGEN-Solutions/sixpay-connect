spring:
  config:
    activate:
      on-profile: amplitude-payment-sandbox
    import:
      - classpath:application-payment-banking.yml
      - classpath:application-payment-banking-reservation.yml
